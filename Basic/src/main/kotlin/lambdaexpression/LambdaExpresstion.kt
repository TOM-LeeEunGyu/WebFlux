package lambdaexpression

/**
 * 인라인 클래스
 * 인라인 클래스를 사용하면 성능적인 오버헤드 없이 타입을 래핑할 수 있다.
 * 타입 시스템을 통해 실수로 코드를 잘못 작성하는 것을 막아준다.
 * 코드의 안정성을 향상시켜준다.
 * 의미가 명확하지 않은 타입, 특히 여러측정 단위를 함께 사용하는 경우에는 인라인 클래스를 꼭 사용하라
 */

/**
 * 일급 함수
 * 일급 함수는 함수의 객체로 취급 될 수 있다.
 * 일급 함수는 함수 객체를 인자로 넘길 수 있다.
 * 일급 함수는 함수의 return 값이 될 수 있다.
 * 런타임에 생성 될 수 있다.
 */